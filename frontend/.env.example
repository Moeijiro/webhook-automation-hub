# Only needed when the dashboard is not served behind the same origin as the
# API. In development the Vite dev server proxies /api and /hooks.
VITE_API_BASE_URL=

# Where the dev-server proxy forwards requests (default http://localhost:8000).
VITE_API_TARGET=http://localhost:8000
