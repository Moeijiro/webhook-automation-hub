# Dashboard

React + TypeScript + Vite front end for the Webhook Automation Hub.
See the [project README](../README.md) for setup; the short version:

```bash
npm install
npm run dev      # http://localhost:5173, proxies /api/ and /hooks/ to :8000
npm run build    # type-check + production build
npm run lint
```
